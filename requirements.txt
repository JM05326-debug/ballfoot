# ---- 第一階段（資料收集/清洗/特徵工程）+ 第二階段（模糊邏輯）+ 第三階段（ML/Poisson）----
pandas>=2.0
numpy>=1.24
requests>=2.31
scikit-learn>=1.4      # Logistic Regression / Random Forest / 評估指標
scipy>=1.11             # Dixon-Coles Poisson 比分模型（MLE 估計、poisson pmf）
joblib>=1.3             # 模型持久化
tabulate>=0.9            # MODEL_CARD.md 的 Markdown 表格輸出

# 梯度提升模型：三者皆為「環境允許時」使用，若安裝失敗，train.py 會自動跳過
# 並在畫面上明確印出「套件未安裝於目前環境」，不會假裝訓練成功。
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# 模糊邏輯層 (src/fuzzy_model.py) 為求穩定與效能，改用手刻的 Mamdani 推論
# （隸屬函數 + 規則庫 + centroid 解模糊），不依賴 scikit-fuzzy / networkx，
# 因此不需要額外安裝該套件。

# ---- 後續階段會用到，先列出，尚未使用前可不安裝 ----
# matplotlib        # 校準曲線 / backtest 圖表
